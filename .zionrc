#!/bin/bash

##
# this
# ----
# a Zion(rc) script file
##

set -e

case "$1" in
    release|publish)
        git add .
        git commit -am "$1"
        git push
        ;;
    help|*)
        echo "Wrong command '$1', use one of this: release, publish."
        ;;
esac
